## 二、种子信息服务模块

### 2.1 种子推广信息管理

#### 2.1.1 推广内容列表查询
- 请求方式：GET
- 请求URL：`/seed/promotion/list`
- 请求参数（Query）：
  - enterpriseId：企业ID（必填）
  - title：推广标题（可选，模糊查询）
  - pageNum：页码（默认1）
  - pageSize：每页数量（默认10）
- 响应格式（JSON）：
```json
{
  "code": 0,
  "msg": "",
  "rows": [
    {
      "promotionId": "PROMO8F2E459A7D3B48",
      "enterpriseId": "ENT1A3B5C7D9E8F7890",
      "title": "2024年优质水稻新品种推广",
      "videoUrl": "/upload/seed/promotion/202405/8f2e459a.mp4",
      "promotionSummary": "该品种抗倒伏、产量高，适合长江流域种植...",
      "recommendedVarieties": "籼稻1号,粳稻3号",
      "publishTime": "2024-05-20 10:30:00",
      "validPeriod": 90,
      "shareLink": "https://domain.com/seed/promotion/PROMO8F2E459A7D3B48",
      "visitCount": 125
    }
  ],
  "total": 50
}
```

#### 2.1.2 推广内容上传（含链接生成）
- 请求方式：POST
- 请求URL：`/seed/promotion/upload`
- 请求参数（FormData格式）：
  - enterpriseId：企业ID（必填）
  - title：推广标题（必填）
  - videoFile：视频文件（必填，仅支持MP4格式，≤100MB）
  - promotionSummary：推广摘要（可选）
  - recommendedVarieties：推荐品种（必填，多个品种用英文逗号分隔）
  - validPeriod：有效期天数（必填）
- 响应格式（JSON）：
```json
{
  "code": 200,
  "msg": "上传成功",
  "data": {
    "promotionId": "PROMO8F2E459A7D3B48",
    "shareLink": "https://domain.com/seed/promotion/PROMO8F2E459A7D3B48"
  }
}
```

#### 2.1.3 访问次数更新（链接访问触发）
- 请求方式：POST
- 请求URL：`/seed/promotion/visit/{promotionId}`
- 请求参数：
  - promotionId：推广ID（路径参数）
- 响应格式（JSON）：
```json
{
  "code": 200,
  "msg": "更新成功",
  "data": {
    "visitCount": 126
  }
}
```

---

### 2.2 种子品种公示查询

#### 2.2.1 品种公示列表查询
- 请求方式：GET
- 请求URL：`/seed/variety/public/list`
- 请求参数（Query）：
  - varietyName：品种名称（可选，模糊查询）
  - year：发布年度（可选，4位数字）
  - cropType：作物类型（可选）
  - pageNum：页码（默认1）
  - pageSize：每页数量（默认10）
- 响应格式（JSON）：
```json
{
  "code": 0,
  "msg": "",
  "rows": [
    {
      "publishId": "7e1d2c4b-8f7e-6543-2109-fedcba098765",
      "varietyName": "籼稻1号",
      "varietyType": "水稻",
      "enterpriseName": "XX种业有限公司",
      "registerNo": "VAR202405001ABC123"
    }
  ],
  "total": 20
}
```

#### 2.2.2 品种详情查询
- 请求方式：GET
- 请求URL：`/seed/variety/public/detail/{publishId}`
- 请求参数：
  - publishId：品种发布ID（路径参数）
- 响应格式（JSON）：
```json
{
  "code": 200,
  "msg": "查询成功",
  "data": {
    "publishId": "7e1d2c4b-8f7e-6543-2109-fedcba098765",
    "varietyName": "籼稻1号",
    "varietyType": "水稻",
    "enterpriseName": "XX种业有限公司",
    "registerNo": "VAR202405001ABC123",
    "baseInfo": "该品种全生育期135天，亩产约650公斤，抗倒伏性强...",
    "photoUrl": "/upload/seed/variety/202405/7e1d2c4b.jpg"
  }
}
```

#### 2.2.3 记录查询行为
- 请求方式：POST
- 请求URL：`/seed/variety/public/record`
- 请求参数（JSON）：
```json
{
  "queryKeyword": "籼稻1号",
  "ipAddress": "192.168.1.100",
  "queryResultCount": 1,
  "viewedPublishId": "7e1d2c4b-8f7e-6543-2109-fedcba098765"
}
```
- 响应格式（JSON）：
```json
{
  "code": 200,
  "msg": "记录成功",
  "data": {
    "queryId": "QUERY9D8C7B6A0F1E2D"
  }
}
```

---

## 三、接口响应状态码说明

| 状态码  | 说明   | 备注                |
| ---- | ---- | ----------------- |
| 200  | 操作成功 | 请求正常处理            |
| 500  | 操作失败 | 服务端错误，msg字段包含错误详情 |

## 四、通用说明

### 4.1 时间格式
- 日期格式：`yyyy-MM-dd`（如：2025-01-01）
- 日期时间格式：`yyyy-MM-dd HH:mm:ss`（如：2025-01-01 10:00:00）

### 4.2 分页参数（适用于列表查询接口）
- pageNum：页码（默认1）
- pageSize：每页数量（默认10）

### 4.3 响应数据结构
#### 4.3.1 普通接口响应
普通接口响应遵循以下结构：
```json
{
  "code": 200,
  "msg": "操作描述",
  "data": "响应数据（可为null、对象或数组）"
}
```

#### 4.3.2 分页列表接口响应
分页列表接口（TableDataInfo）响应遵循以下结构：
```json
{
  "code": 0,
  "msg": "",
  "rows": [      // 数据列表
    // 具体数据对象
  ],
  "total": 100  // 总记录数
}
```